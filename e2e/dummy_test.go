package e2e

import "testing"

func TestE2EDummy(t *testing.T) {
	// This dummy test prevents "build constraints exclude all Go files"
	// when running tests via IDE without the 'integration' build tag.
}
